{% extends 'base.html.twig' %}

{% block title %}Crud{% endblock %}

{% block body %}
    <h1>Crud</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crud.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ crud.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ crud.date ? crud.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ crud.description }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ crud.picture }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ crud.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud2_index') }}">back to list</a>

    <a href="{{ path('app_crud2_edit', {'id': crud.id}) }}">edit</a>

    {{ include('crud2/_delete_form.html.twig') }}
{% endblock %}
